{
  "id": "dll-依赖分析与收集工具",
  "title": "DLL 依赖分析与收集工具",
  "date": "2026-06-29",
  "tags": [
    "dll",
    "python"
  ],
  "summary": "完爆了以前的dependency，我服了，以前过的什么苦日子",
  "content": "```bash\n  # 分析依赖 - 树形显示（只看非系统 DLL）\n  python dll_tool.py analyze SDM_Sys.dll --tree\n\n  # 分析依赖 - 显示全部（含系统 DLL）\n  python dll_tool.py analyze SDM_Sys.dll --tree --show-system\n\n  # 显示扫描过程详情\n  python dll_tool.py analyze SDM_Sys.dll --tree -v\n\n  # 收集全部依赖到目录\n  python dll_tool.py collect SDM_Sys.dll ftd2xx.dll -o ./dll_pack\n\n  # 只收集非系统 DLL（推荐，避免一堆 Windows 自带的）\n  python dll_tool.py collect SDM_Sys.dll ftd2xx.dll -o ./dll_pack --skip-system\n\n  # 先预览再收集\n  python dll_tool.py collect SDM_Sys.dll -o ./dll_pack --list\n\n  文件顶部有完整的 PE 格式原理注释，包括：\n  - PE 文件结构图（DOS Header → COFF → Optional Header → Sections）\n  - Import Table 的结构和 RVA 转换原理\n  - API Set 虚拟 DLL 的说明\n  - 各种注意事项\n```\n\n```python\n\"\"\"\nDLL 依赖分析与收集工具\n======================\n\n功能:\n  1. 分析指定 DLL/EXE 的全部依赖（递归）\n  2. 将所有依赖收集到一个目录，方便在其他机器上部署\n\n原理:\n  Windows PE (Portable Executable) 文件的导入表 (Import Table) 记录了该文件\n  运行时需要加载哪些外部 DLL，以及从每个 DLL 中导入哪些函数。\n\n  PE 文件结构概览:\n  ┌──────────────────────────────────┐\n  │ DOS Header (64 bytes)            │  偏移 0x3C 处存储 PE 签名的文件偏移\n  ├──────────────────────────────────┤\n  │ PE Signature \"PE\\0\\0\"            │  PE 文件标识\n  ├──────────────────────────────────┤\n  │ COFF Header (20 bytes)           │  Machine(架构), NumSections, SizeOfOptionalHeader\n  ├──────────────────────────────────┤\n  │ Optional Header                  │  Magic(PE32/PE32+), 数据目录(DataDirectory)...\n  │   DataDirectory[0] Export Table  │  导出的函数列表\n  │   DataDirectory[1] Import Table  │  ★ 导入的 DLL 和函数列表\n  │   DataDirectory[2] Resource      │\n  │   ...                            │\n  ├──────────────────────────────────┤\n  │ Section Table                    │  节表，记录每个节的 RVA/文件偏移映射\n  ├──────────────────────────────────┤\n  │ .text  (代码)                    │\n  │ .rdata (只读数据，含导入表)       │\n  │ .data  (可读写数据)              │\n  │ .rsrc  (资源)                    │\n  │ .reloc (重定位)                  │\n  └──────────────────────────────────┘\n\n  Import Table 的结构:\n  每个导入的 DLL 对应一个 IMAGE_IMPORT_DESCRIPTOR (20 bytes):\n    - OriginalFirstThunk (ILT RVA): 指向导入名称/序号表\n    - Name RVA:           指向 DLL 名称字符串\n    - FirstThunk (IAT):   实际填充导入地址的表\n\n  RVA (Relative Virtual Address) 是内存中的相对地址，需要通过节表\n  转换为文件偏移才能从磁盘文件中读取对应数据。\n\n  转换公式: file_offset = RVA - Section.VirtualAddress + Section.PointerToRawData\n\n注意事项:\n  - api-ms-win-* / ext-ms-* 开头的是 Windows API Set（虚拟 DLL），\n    不是真实文件，由 Windows 内核透明处理，不需要收集。\n  - KERNEL32.dll, NTDLL.dll 等系统核心 DLL 在所有 Windows 上都有，\n    一般不需要收集。\n  - 32 位 DLL 依赖的系统 DLL 在 System32 (SysWOW64) 中，\n    64 位 DLL 依赖的系统 DLL 在 System32 中。\n\n用法:\n  # 分析依赖（仅查看，不复制）\n  python dll_tool.py analyze <dll_path>\n\n  # 分析并显示树形结构\n  python dll_tool.py analyze <dll_path> --tree\n\n  # 收集所有依赖到指定目录\n  python dll_tool.py collect <dll_path> -o <output_dir>\n\n  # 收集多个入口文件的依赖\n  python dll_tool.py collect <dll1> <dll2> -o <output_dir>\n\n  # 只收集非系统 DLL（推荐，避免收集一堆 Windows 自带的）\n  python dll_tool.py collect <dll_path> -o <output_dir> --skip-system\n\n  # 显示详细扫描过程\n  python dll_tool.py analyze <dll_path> -v\n\"\"\"\n\nimport os\nimport sys\nimport shutil\nimport ctypes\nimport argparse\nfrom pathlib import Path\n\n# Windows 控制台 UTF-8 输出\nif sys.platform == \"win32\":\n    try:\n        sys.stdout.reconfigure(encoding=\"utf-8\")\n        sys.stderr.reconfigure(encoding=\"utf-8\")\n    except Exception:\n        pass\n\nkernel32 = ctypes.WinDLL(\"kernel32\", use_last_error=True)\n\n# ─── 常量 ────────────────────────────────────────────────────────────────────\n\nMAX_DEPTH = 10  # 递归深度上限，防止循环依赖导致无限展开\n\n# Windows API Set 虚拟 DLL 前缀，不是真实文件\nAPI_SET_PREFIXES = (\"api-ms-\", \"ext-ms-\")\n\n# Windows 系统目录（这些目录下的 DLL 通常不需要收集）\nSYSTEM_DIRS = []\nfor _env in (\"SystemRoot\", \"WINDIR\"):\n    _root = os.environ.get(_env)\n    if _root:\n        SYSTEM_DIRS.append(os.path.join(_root, \"System32\").lower())\n        SYSTEM_DIRS.append(os.path.join(_root, \"SysWOW64\").lower())\nSYSTEM_DIRS = list(set(SYSTEM_DIRS))\n\n\n# ─── PE 解析 ─────────────────────────────────────────────────────────────────\n\ndef pe_parse_imports(dll_path):\n    \"\"\"\n    解析 PE 文件的导入表，返回该 DLL 直接依赖的 DLL 名称列表。\n\n    解析步骤:\n      1. 读取 DOS Header，找到 PE 签名偏移 (0x3C 处的 e_lfanew)\n      2. 读取 COFF Header，获取节数量和 Optional Header 大小\n      3. 读取 Optional Header 的 Magic 字段判断 PE32/PE32+\n      4. 从 DataDirectory[1] 获取 Import Table 的 RVA 和大小\n      5. 用节表将 RVA 转换为文件偏移\n      6. 遍历 Import Descriptor 数组，读取每个导入 DLL 的名称\n\n    Args:\n        dll_path: DLL/EXE 文件路径\n\n    Returns:\n        (imports, error)\n        - imports: list[str] | None  - 导入的 DLL 名称列表\n        - error: str | None          - 错误信息\n    \"\"\"\n    # ── 读取文件 ──\n    try:\n        with open(dll_path, \"rb\") as f:\n            data = f.read()\n    except Exception as e:\n        return None, f\"读取文件失败: {e}\"\n\n    # ── DOS Header ──\n    # 偏移 0x3C 处是 e_lfanew，指向 PE 签名位置\n    if len(data) < 0x40:\n        return None, \"文件太小，不是有效的 PE\"\n    pe_offset = int.from_bytes(data[0x3C:0x40], \"little\")\n    if pe_offset + 24 > len(data):\n        return None, \"PE 偏移越界\"\n    if data[pe_offset : pe_offset + 4] != b\"PE\\x00\\x00\":\n        return None, \"PE 签名不匹配\"\n\n    # ── COFF Header ──\n    # pe_offset + 6:  NumberOfSections (2 bytes)\n    # pe_offset + 20: SizeOfOptionalHeader (2 bytes)\n    num_sections = int.from_bytes(data[pe_offset + 6 : pe_offset + 8], \"little\")\n    size_of_optional = int.from_bytes(data[pe_offset + 20 : pe_offset + 22], \"little\")\n\n    # ── Optional Header ──\n    opt_off = pe_offset + 24\n    magic = int.from_bytes(data[opt_off : opt_off + 2], \"little\")\n\n    # DataDirectory[1] (Import Table) 在 Optional Header 中的偏移:\n    #   PE32  (0x10b): Optional Header 基址 + 96 (DataDirectory 起始) + 8 (跳过 [0])\n    #   PE32+ (0x20b): Optional Header 基址 + 112 + 8\n    if magic == 0x10B:\n        import_dir_off = opt_off + 96 + 8\n    elif magic == 0x20B:\n        import_dir_off = opt_off + 112 + 8\n    else:\n        return None, f\"未知 PE 格式: 0x{magic:04X}\"\n\n    if import_dir_off + 8 > len(data):\n        return None, \"导入目录越界\"\n\n    # Import Table 的 RVA 和大小\n    import_rva = int.from_bytes(data[import_dir_off : import_dir_off + 4], \"little\")\n    import_size = int.from_bytes(\n        data[import_dir_off + 4 : import_dir_off + 8], \"little\"\n    )\n    if import_rva == 0 or import_size == 0:\n        return [], None  # 没有导入表（比如纯资源 DLL）\n\n    # ── 节表 ──\n    # 节表紧跟在 Optional Header 后面\n    # 每个节表项 40 bytes，结构:\n    #   [0:8]   Name\n    #   [8:12]  VirtualSize\n    #   [12:16] VirtualAddress (RVA)\n    #   [16:20] SizeOfRawData\n    #   [20:24] PointerToRawData (文件偏移)\n    sec_table_off = opt_off + size_of_optional\n\n    def rva_to_offset(rva):\n        \"\"\"将 RVA 转换为文件偏移\"\"\"\n        for i in range(num_sections):\n            sec = sec_table_off + i * 40\n            if sec + 40 > len(data):\n                break\n            vrva = int.from_bytes(data[sec + 12 : sec + 16], \"little\")\n            vsize = int.from_bytes(data[sec + 8 : sec + 12], \"little\")\n            raw_size = int.from_bytes(data[sec + 16 : sec + 20], \"little\")\n            raw_ptr = int.from_bytes(data[sec + 20 : sec + 24], \"little\")\n            # 判断 RVA 是否落在这个节的范围内\n            if vrva <= rva < vrva + max(vsize, raw_size):\n                return rva - vrva + raw_ptr\n        return None\n\n    # ── 解析 Import Descriptor 数组 ──\n    # 每个 IMAGE_IMPORT_DESCRIPTOR 20 bytes:\n    #   [0:4]   OriginalFirstThunk (ILT RVA)\n    #   [4:8]   TimeDateStamp\n    #   [8:12]  ForwarderChain\n    #   [12:16] Name RVA -> 指向 DLL 名称字符串\n    #   [16:20] FirstThunk (IAT RVA)\n    imports = []\n    desc_size = 20\n    offset = rva_to_offset(import_rva)\n    if offset is None:\n        return None, f\"无法将导入表 RVA 0x{import_rva:X} 转换为文件偏移\"\n\n    while offset + desc_size <= len(data):\n        name_rva = int.from_bytes(data[offset + 12 : offset + 16], \"little\")\n        if name_rva == 0:\n            break  # 空描述符，数组结束\n        name_off = rva_to_offset(name_rva)\n        if name_off is not None and name_off < len(data):\n            end = data.find(b\"\\x00\", name_off)\n            if end == -1:\n                end = len(data)\n            name = data[name_off:end].decode(\"utf-8\", errors=\"replace\")\n            if name:\n                imports.append(name)\n        offset += desc_size\n\n    return imports, None\n\n\ndef pe_get_arch(dll_path):\n    \"\"\"\n    判断 PE 文件是 32 位还是 64 位。\n\n    通过 COFF Header 的 Machine 字段判断:\n      0x014C = IMAGE_FILE_MACHINE_I386  (32-bit x86)\n      0x8664 = IMAGE_FILE_MACHINE_AMD64 (64-bit x64)\n    \"\"\"\n    try:\n        with open(dll_path, \"rb\") as f:\n            data = f.read(512)\n        pe_off = int.from_bytes(data[0x3C:0x40], \"little\")\n        machine = int.from_bytes(data[pe_off + 4 : pe_off + 6], \"little\")\n        if machine == 0x014C:\n            return \"x86\"\n        elif machine == 0x8664:\n            return \"x64\"\n        return f\"unknown(0x{machine:04X})\"\n    except Exception:\n        return \"unknown\"\n\n\n# ─── DLL 搜索 ────────────────────────────────────────────────────────────────\n\ndef find_dll(dll_name):\n    \"\"\"\n    在系统中搜索 DLL 文件。\n\n    搜索顺序:\n      1. 脚本所在目录（程序自带的 DLL）\n      2. Windows SearchPath API（按系统 DLL 搜索顺序）\n      3. 手动搜索 System32 / SysWOW64\n\n    Returns:\n        找到的完整路径，或 None\n    \"\"\"\n    name_lower = dll_name.lower()\n\n    # API Set 不是真实文件\n    if name_lower.startswith(API_SET_PREFIXES):\n        return None\n\n    # 1. 脚本同级目录\n    script_dir = os.path.dirname(os.path.abspath(__file__))\n    for sub in (\"\", \"_internal\"):\n        p = os.path.join(script_dir, sub, dll_name)\n        if os.path.isfile(p):\n            return p\n\n    # 2. Windows SearchPath（遵循系统 DLL 搜索顺序）\n    buf = ctypes.create_unicode_buffer(512)\n    if kernel32.SearchPathW(None, dll_name, None, 512, buf, None) > 0:\n        return buf.value\n\n    # 3. 手动搜索常见系统目录\n    for d in SYSTEM_DIRS:\n        p = os.path.join(d, dll_name)\n        if os.path.isfile(p):\n            return p\n\n    return None\n\n\ndef is_system_dll(dll_path):\n    \"\"\"判断 DLL 是否位于系统目录\"\"\"\n    path_lower = os.path.normpath(dll_path).lower()\n    return any(path_lower.startswith(sd) for sd in SYSTEM_DIRS)\n\n\n# ─── 依赖扫描 ────────────────────────────────────────────────────────────────\n\ndef scan_deps(entry_path, verbose=False):\n    \"\"\"\n    递归扫描一个入口文件的全部依赖。\n\n    Args:\n        entry_path: 入口 DLL/EXE 的路径\n        verbose: 是否打印扫描过程\n\n    Returns:\n        list[dict] - 每个元素:\n          - name:  DLL 文件名\n          - path:  完整路径\n          - size:  文件大小\n          - depth: 在依赖树中的深度\n          - is_system: 是否为系统 DLL\n    \"\"\"\n    visited = set()\n    collected = []\n\n    def _scan(dll_path, depth):\n        name_lower = os.path.basename(dll_path).lower()\n        if name_lower in visited:\n            return\n        visited.add(name_lower)\n\n        collected.append(\n            {\n                \"name\": os.path.basename(dll_path),\n                \"path\": dll_path,\n                \"size\": os.path.getsize(dll_path),\n                \"depth\": depth,\n                \"is_system\": is_system_dll(dll_path),\n            }\n        )\n\n        imports, err = pe_parse_imports(dll_path)\n        if err:\n            if verbose:\n                print(f\"{'  ' * depth}[!] {os.path.basename(dll_path)}: {err}\")\n            return\n\n        # 过滤 API Set\n        real_imports = [\n            d for d in imports if not d.lower().startswith(API_SET_PREFIXES)\n        ]\n\n        if verbose:\n            print(\n                f\"{'  ' * depth}[+] {os.path.basename(dll_path)} -> {len(real_imports)} deps\"\n            )\n\n        for dep in real_imports:\n            dep_path = find_dll(dep)\n            if dep_path:\n                if os.path.basename(dep_path).lower() not in visited:\n                    if depth + 1 >= MAX_DEPTH:\n                        if verbose:\n                            print(f\"{'  ' * (depth + 1)}[-] {dep} (depth limit)\")\n                    else:\n                        _scan(dep_path, depth + 1)\n            else:\n                if verbose:\n                    print(f\"{'  ' * (depth + 1)}[?] {dep} (not found)\")\n\n    _scan(entry_path, 0)\n    return collected\n\n\ndef deduplicate(results):\n    \"\"\"按文件名去重（保留第一个找到的）\"\"\"\n    seen = set()\n    unique = []\n    for item in results:\n        key = item[\"name\"].lower()\n        if key not in seen:\n            seen.add(key)\n            unique.append(item)\n    return unique\n\n\n# ─── 输出格式 ─────────────────────────────────────────────────────────────────\n\ndef fmt_size(size):\n    \"\"\"格式化文件大小\"\"\"\n    if size < 1024:\n        return f\"{size} B\"\n    if size < 1024 * 1024:\n        return f\"{size / 1024:.1f} KB\"\n    return f\"{size / (1024 * 1024):.1f} MB\"\n\n\ndef print_table(results, show_system=True):\n    \"\"\"表格形式打印\"\"\"\n    items = results if show_system else [r for r in results if not r[\"is_system\"]]\n    print(f\"\\n{'DLL':45s} {'Size':>10s}  {'Type':6s}  Path\")\n    print(\"-\" * 120)\n    for item in items:\n        tag = \"SYS\" if item[\"is_system\"] else \"USER\"\n        indent = \"  \" * item.get(\"depth\", 0)\n        print(\n            f\"{indent}{item['name']:43s} {fmt_size(item['size']):>10s}  {tag:6s}  {item['path']}\"\n        )\n    print(f\"\\nTotal: {len(items)} DLLs\")\n\n\ndef print_tree(results, show_system=True):\n    \"\"\"树形结构打印\"\"\"\n    items = results if show_system else [r for r in results if not r[\"is_system\"]]\n    print()\n    for item in items:\n        depth = item.get(\"depth\", 0)\n        if depth == 0:\n            prefix = \"#\"\n        else:\n            prefix = \"|-- \" if depth == 1 else \"|   \" * (depth - 1) + \"|-- \"\n        tag = \" [SYS]\" if item[\"is_system\"] else \"\"\n        print(f\"{prefix}{item['name']}  ({fmt_size(item['size'])}){tag}\")\n    print(f\"\\nTotal: {len(items)} DLLs\")\n\n\n# ─── 子命令 ───────────────────────────────────────────────────────────────────\n\ndef cmd_analyze(args):\n    \"\"\"分析依赖\"\"\"\n    entry = os.path.abspath(args.entry)\n    if not os.path.isfile(entry):\n        print(f\"Error: file not found: {entry}\")\n        sys.exit(1)\n\n    arch = pe_get_arch(entry)\n    print(f\"Analyzing: {entry} ({arch})\")\n\n    results = scan_deps(entry, verbose=args.verbose)\n    results = deduplicate(results)\n\n    if args.tree:\n        print_tree(results, show_system=args.show_system)\n    else:\n        print_table(results, show_system=args.show_system)\n\n\ndef cmd_collect(args):\n    \"\"\"收集依赖\"\"\"\n    entries = [os.path.abspath(e) for e in args.entries]\n    for e in entries:\n        if not os.path.isfile(e):\n            print(f\"Error: file not found: {e}\")\n            sys.exit(1)\n\n    print(\"Scanning dependencies...\")\n    all_results = []\n    for entry in entries:\n        arch = pe_get_arch(entry)\n        print(f\"  {os.path.basename(entry)} ({arch})\")\n        results = scan_deps(entry, verbose=args.verbose)\n        all_results.extend(results)\n\n    results = deduplicate(all_results)\n\n    # --skip-system: 过滤掉系统目录中的 DLL\n    if args.skip_system:\n        before = len(results)\n        results = [r for r in results if not r[\"is_system\"]]\n        print(f\"Filtered: {before} -> {len(results)} (removed system DLLs)\")\n\n    total_size = sum(r[\"size\"] for r in results)\n    print(f\"\\nFound {len(results)} DLLs, total size {fmt_size(total_size)}\")\n\n    # --list 模式: 只看不复制\n    if args.list:\n        print_table(results, show_system=True)\n        return\n\n    # 复制\n    output_dir = os.path.abspath(args.output)\n    os.makedirs(output_dir, exist_ok=True)\n    print(f\"\\nCopying to: {output_dir}\")\n    print(\"-\" * 80)\n\n    copied, skipped, failed = 0, 0, 0\n    for item in results:\n        dst = os.path.join(output_dir, item[\"name\"])\n        try:\n            if os.path.isfile(dst) and os.path.getsize(dst) == item[\"size\"]:\n                skipped += 1\n            else:\n                shutil.copy2(item[\"path\"], dst)\n                tag = \" [SYS]\" if item[\"is_system\"] else \"\"\n                print(f\"  + {item['name']:45s} {fmt_size(item['size']):>10s}{tag}\")\n                copied += 1\n        except Exception as e:\n            print(f\"  x {item['name']:45s} FAILED: {e}\")\n            failed += 1\n\n    print(\"-\" * 80)\n    print(f\"Done: copied {copied}, skipped {skipped}, failed {failed}\")\n    print(f\"Output: {output_dir}\")\n\n    # 生成清单\n    manifest_path = os.path.join(output_dir, \"_manifest.txt\")\n    with open(manifest_path, \"w\", encoding=\"utf-8\") as f:\n        f.write(\"# DLL Dependency Manifest\\n\")\n        f.write(f\"# Collected from: {os.environ.get('COMPUTERNAME', 'unknown')}\\n\")\n        f.write(f\"# Total: {len(results)}, Size: {fmt_size(total_size)}\\n\\n\")\n        for item in results:\n            tag = \" [SYS]\" if item[\"is_system\"] else \"\"\n            f.write(f\"{item['name']}\\t{item['size']}\\t{tag}\\t{item['path']}\\n\")\n    print(f\"Manifest: {manifest_path}\")\n\n\n# ─── 主入口 ───────────────────────────────────────────────────────────────────\n\ndef main():\n    parser = argparse.ArgumentParser(\n        description=\"DLL dependency analyzer and collector\",\n        formatter_class=argparse.RawDescriptionHelpFormatter,\n        epilog=__doc__,\n    )\n    sub = parser.add_subparsers(dest=\"command\")\n\n    # analyze 子命令\n    p_analyze = sub.add_parser(\"analyze\", help=\"Analyze DLL dependencies\")\n    p_analyze.add_argument(\"entry\", help=\"DLL or EXE to analyze\")\n    p_analyze.add_argument(\"--tree\", action=\"store_true\", help=\"Show as tree\")\n    p_analyze.add_argument(\n        \"--show-system\", action=\"store_true\", help=\"Include system DLLs\"\n    )\n    p_analyze.add_argument(\"-v\", \"--verbose\", action=\"store_true\", help=\"Show scan details\")\n\n    # collect 子命令\n    p_collect = sub.add_parser(\"collect\", help=\"Collect all dependencies\")\n    p_collect.add_argument(\"entries\", nargs=\"+\", help=\"DLL/EXE files to collect\")\n    p_collect.add_argument(\"-o\", \"--output\", default=\"./dll_pack\", help=\"Output dir\")\n    p_collect.add_argument(\"--list\", action=\"store_true\", help=\"List only, don't copy\")\n    p_collect.add_argument(\n        \"--skip-system\", action=\"store_true\", help=\"Skip system DLLs\"\n    )\n    p_collect.add_argument(\n        \"--show-system\", action=\"store_true\", help=\"Show system DLLs in list mode\"\n    )\n    p_collect.add_argument(\"-v\", \"--verbose\", action=\"store_true\", help=\"Show scan details\")\n\n    args = parser.parse_args()\n\n    if args.command == \"analyze\":\n        cmd_analyze(args)\n    elif args.command == \"collect\":\n        cmd_collect(args)\n    else:\n        parser.print_help()\n\n\nif __name__ == \"__main__\":\n    main()\n\n```",
  "draft": false
}